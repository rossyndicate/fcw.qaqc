% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_flag_column.R
\name{tidy_flag_column}
\alias{tidy_flag_column}
\title{Clean and standardize quality flag formatting}
\usage{
tidy_flag_column(df)
}
\arguments{
\item{df}{A dataframe containing water quality flags. Must include an \code{auto_flag}
column containing quality flag text.}
}
\value{
A dataframe with the same structure as the input, but with the \code{auto_flag}
column standardized and cleaned for consistency.
}
\description{
Standardizes and formats the quality flags in the \code{auto_flag} column to ensure
consistency across the dataset. This function handles several formatting tasks:
\enumerate{
\item Converts various flag formats to standardized versions
\item Prioritizes certain flag types over others
\item Removes trailing and leading semicolons
\item Sorts multiple flags in a consistent order
\item Replaces newlines with spaces
}
}
\examples{
# Clean quality flags across all parameters and sites
final_flags <- intersensor_flags \%>\%
  dplyr::bind_rows() \%>\%
  tidy_flag_column() \%>\%
  split(f = list(.$site, .$parameter), sep = "-")
}
